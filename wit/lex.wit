package elewis787:lex@0.0.1;

interface word {
    count: func(s: string, sub: string) -> u32;
}

world guest {  
    export word;
}

world host {
    import word; 
}

world lex {
    include guest;
    include host; 
}